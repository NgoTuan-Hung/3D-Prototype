using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(SkillableObject), typeof(MoveToTarget), typeof(MeleeSimpleAttackWhenNear))]
public class MeleeSkeleton : CustomMonoBehavior
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
